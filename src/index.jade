doctype html
html
    head
        title Notedown!
body
    div(ng-controller="NoteDownController as vm")
        p is this production?
        p= production
        p {{ vm.hello }}
        p {{ vm.test('omg! ') }}
        p {{ vm.classTest.sayHello() }}
        p {{ vm.someObject.sayOne() }} 
        p {{ vm.someObject.sayOne(5) }} 
        p
            button(ng-click="vm.updateHello()") update!
            button(ng-click="vm.someObject.sayOne(132)") say something
            button(ng-click="vm.spreadFunction([1, 3, 2, 'hello'])") try spread
    if production
        script(type="text/javascript" src="app.js")
    else
        script(type="text/javascript" src="jspm_packages/system.js")
        script(type="text/javascript" src="config.js")
        script(type="text/javascript" src="bootstrap.js")
